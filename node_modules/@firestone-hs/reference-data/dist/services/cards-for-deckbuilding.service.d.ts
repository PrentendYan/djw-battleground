import { Sideboard } from '@firestone-hs/deckstrings';
import { GameFormat } from '../public-api';
import { AllCardsService } from './all-cards.service';
export declare class CardsForDeckbuildingService {
    private duplicatesForDeckbuilding;
    init(allCards: AllCardsService): void;
    normalizeDeckList(decklist: string, allCards: AllCardsService): string;
    normalizeSideboard(sideboard: Sideboard, format: GameFormat, allCards: AllCardsService): Sideboard;
    getBaseCardDbfIdForDeckbuilding(cardIdOrDbfId: number | string, format: GameFormat, allCards: AllCardsService): number;
    getBaseCardIdForDeckbuilding(cardIdOrDbfId: number | string, format: GameFormat, allCards: AllCardsService): string;
    private getAllowedSets;
}
