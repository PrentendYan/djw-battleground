"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimewarpedConch = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const utils_1 = require("../../../services/utils");
const add_minion_to_board_1 = require("../../../simulation/add-minion-to-board");
const deathrattle_spawns_1 = require("../../../simulation/deathrattle-spawns");
const spawns_1 = require("../../../simulation/spawns");
const utils_2 = require("../../../utils");
exports.TimewarpedConch = {
    cardIds: ["BG34_Treasure_951"],
    castTavernSpell: (spellCardId, input) => {
        var _a;
        const target = (_a = input.target) !== null && _a !== void 0 ? _a : (0, utils_1.pickRandom)(input.board.filter((e) => (0, utils_2.hasCorrectTribe)(e, input.hero, reference_data_1.Race.MURLOC, input.gameState.anomalies, input.gameState.allCards)));
        if (!!target) {
            const copy = (0, utils_2.copyEntity)(target);
            copy.entityId = input.gameState.sharedState.currentEntityId++;
            (0, add_minion_to_board_1.removeAurasFromSelf)(copy, input.board, input.hero, input.gameState);
            const newMinions = (0, deathrattle_spawns_1.spawnEntities)(copy.cardId, 1, input.board, input.hero, input.otherBoard, input.otherHero, input.gameState, input.hero.friendly, false, false, false, copy);
            const spawns = (0, spawns_1.performEntitySpawns)(newMinions, input.board, input.hero, target, input.board.length - input.board.indexOf(target) - 1, input.otherBoard, input.otherHero, input.gameState);
            input.gameState.spectator.registerPowerTarget(input.source, target, input.board, input.hero, input.otherHero);
        }
    },
};
//# sourceMappingURL=timewarped-conch.js.map