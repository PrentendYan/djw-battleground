import { BoardSecret } from '../../../board-secret';
import { SoCInput } from '../../../simulation/start-of-combat/start-of-combat-input';
export declare const UpperHand: {
    startOfCombat: (trinket: BoardSecret, input: SoCInput) => boolean;
};
