"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AllSeeingElder = void 0;
const stats_1 = require("../../../simulation/stats");
exports.AllSeeingElder = {
    cardIds: ["BG33_300", "BG33_300_G"],
    startOfCombat: (minion, input) => {
        const multiplier = minion.cardId === "BG33_300_G" ? 2 : 1;
        const attackToGain = input.playerBoard[0].attack;
        const healthToGain = input.playerBoard[input.playerBoard.length - 1].health;
        for (let i = 0; i < multiplier; i++) {
            (0, stats_1.modifyStats)(minion, minion, attackToGain, healthToGain, input.playerBoard, input.playerEntity, input.gameState);
            input.gameState.spectator.registerPowerTarget(minion, minion, input.playerBoard, input.playerEntity, input.opponentEntity);
        }
        return true;
    },
};
//# sourceMappingURL=all-seeing-elder.js.map