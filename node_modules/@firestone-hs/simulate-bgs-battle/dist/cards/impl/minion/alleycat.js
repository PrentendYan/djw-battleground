"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Alleycat = void 0;
const deathrattle_spawns_1 = require("../../../simulation/deathrattle-spawns");
exports.Alleycat = {
    cardIds: ["BG_CFM_315", "TB_BaconUps_093"],
    battlecry: (minion, input) => {
        const spawnInput = {
            boardWithDeadEntity: input.board,
            boardWithDeadEntityHero: input.hero,
            gameState: input.gameState,
            deadEntity: minion,
            otherBoard: input.otherBoard,
            otherBoardHero: input.otherHero,
        };
        const cardId = minion.cardId === "BG_CFM_315"
            ? "BG_CFM_315t"
            : "TB_BaconUps_093t";
        const indexFromRight = input.board.length - input.board.indexOf(minion) - 1;
        (0, deathrattle_spawns_1.simplifiedSpawnEntitiesWithAddToBoard)(cardId, 1, spawnInput, minion, indexFromRight);
        return true;
    },
};
//# sourceMappingURL=alleycat.js.map