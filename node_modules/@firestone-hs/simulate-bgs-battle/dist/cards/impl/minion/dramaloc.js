"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Dramaloc = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const utils_1 = require("../../../services/utils");
const stats_1 = require("../../../simulation/stats");
const utils_2 = require("../../../utils");
exports.Dramaloc = {
    cardIds: ["BG34_143", "BG34_143_G"],
    rally: (minion, input) => {
        var _a, _b;
        if (!((_a = input.attackingHero) === null || _a === void 0 ? void 0 : _a.hand)) {
            return { dmgDoneByAttacker: 0, dmgDoneByDefender: 0 };
        }
        const mult = minion.cardId === "BG34_143_G" ? 2 : 1;
        const highestAttackInHand = (_b = input.attackingHero.hand
            .filter((e) => e.attack != null && !!e.cardId)
            .sort((a, b) => b.attack - a.attack)[0]) === null || _b === void 0 ? void 0 : _b.attack;
        const statsSource = (0, utils_1.pickRandom)(input.attackingHero.hand.filter((e) => e.attack == highestAttackInHand && !!e.cardId));
        if (!!statsSource) {
            for (let i = 0; i < mult; i++) {
                const candidates = input.attackingBoard.filter((e) => e !== minion &&
                    (0, utils_2.hasCorrectTribe)(e, input.attackingHero, reference_data_1.Race.MURLOC, input.gameState.anomalies, input.gameState.allCards));
                const targets = (0, utils_1.pickMultipleRandom)(candidates, 2);
                for (const target of targets) {
                    (0, stats_1.modifyStats)(target, minion, statsSource.attack, statsSource.health, input.attackingBoard, input.attackingHero, input.gameState);
                }
            }
        }
        return { dmgDoneByAttacker: 0, dmgDoneByDefender: 0 };
    },
};
//# sourceMappingURL=dramaloc.js.map