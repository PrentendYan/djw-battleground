"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Felhorn = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const attack_1 = require("../../../simulation/attack");
const stats_1 = require("../../../simulation/stats");
const utils_1 = require("../../../utils");
exports.Felhorn = {
    cardIds: ["BG34_781", "BG34_781_G"],
    battlecry: (minion, input) => {
        const loops = minion.cardId === "BG34_781_G" ? 4 : 2;
        for (let i = 0; i < loops; i++) {
            const targets = input.board.filter((e) => e != minion &&
                ((0, utils_1.hasCorrectTribe)(e, input.hero, reference_data_1.Race.BEAST, input.gameState.anomalies, input.gameState.allCards) ||
                    (0, utils_1.hasCorrectTribe)(e, input.hero, reference_data_1.Race.DEMON, input.gameState.anomalies, input.gameState.allCards)));
            for (const target of targets) {
                (0, stats_1.modifyStats)(target, minion, 1, 2, input.board, input.hero, input.gameState);
                (0, attack_1.dealDamageToMinion)(target, input.board, input.hero, minion, 1, input.otherBoard, input.otherHero, input.gameState);
            }
        }
        return true;
    },
};
//# sourceMappingURL=felhorn.js.map