"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MawCaster = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const utils_1 = require("../../../services/utils");
const cards_in_hand_1 = require("../../../simulation/cards-in-hand");
const utils_2 = require("../../../utils");
exports.MawCaster = {
    cardIds: ["BG32_340", "BG32_340_G"],
    battlecry: (minion, input) => {
        const undead = input.board.filter((e) => (0, utils_2.hasCorrectTribe)(e, input.hero, reference_data_1.Race.UNDEAD, input.gameState.anomalies, input.gameState.allCards));
        const target = (0, utils_1.pickRandomAlive)(undead);
        if (!target) {
            return;
        }
        target.definitelyDead = true;
        const mult = minion.cardId === "BG32_340_G" ? 2 : 1;
        for (let i = 0; i < mult; i++) {
            const card = input.gameState.cardsData.getRandomMinionForTribe(reference_data_1.Race.UNDEAD, input.hero.tavernTier);
            (0, cards_in_hand_1.addCardsInHand)(input.hero, input.board, [card], input.gameState);
        }
        return true;
    },
};
//# sourceMappingURL=maw-caster.js.map