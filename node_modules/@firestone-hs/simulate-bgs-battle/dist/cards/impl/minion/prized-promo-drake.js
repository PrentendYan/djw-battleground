"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PrizedPromoDrake = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const stats_1 = require("../../../simulation/stats");
const utils_1 = require("../../../utils");
exports.PrizedPromoDrake = {
    cardIds: ["BG21_014", "BG21_014_G"],
    startOfCombatTiming: 'start-of-combat',
    startOfCombat: (minion, input) => {
        const mult = minion.cardId === "BG21_014_G" ? 2 : 1;
        const targets = input.playerBoard.filter((e) => (0, utils_1.hasCorrectTribe)(e, input.playerEntity, reference_data_1.Race.DRAGON, input.gameState.anomalies, input.gameState.allCards));
        if (!!targets.length) {
            for (const entity of targets) {
                (0, stats_1.modifyStats)(entity, minion, 4 * mult, 4 * mult, input.playerBoard, input.playerEntity, input.gameState);
            }
            return true;
        }
    },
};
//# sourceMappingURL=prized-promo-drake.js.map