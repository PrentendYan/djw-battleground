"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Psychus = void 0;
const stats_1 = require("../../../simulation/stats");
exports.Psychus = {
    cardIds: ["BG34_318", "BG34_318_G"],
    startOfCombat: (minion, input) => {
        const multiplier = minion.cardId === "BG34_318_G" ? 2 : 1;
        const attackToGain = Math.max(...[...input.playerBoard, ...input.opponentBoard].map((e) => e.attack));
        const healthToGain = Math.max(...[...input.playerBoard, ...input.opponentBoard].map((e) => e.health));
        (0, stats_1.setEntityStats)(minion, attackToGain * multiplier, healthToGain * multiplier, input.playerBoard, input.playerEntity, input.gameState);
        input.gameState.spectator.registerPowerTarget(minion, minion, input.playerBoard, input.playerEntity, input.opponentEntity);
        return true;
    },
};
//# sourceMappingURL=psychus.js.map