"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Rampager = void 0;
const attack_1 = require("../../../simulation/attack");
exports.Rampager = {
    cardIds: ["BG29_809", "BG29_809_G"],
    rally: (minion, input) => {
        const loops = input.attacker.cardId === "BG29_809_G" ? 2 : 1;
        for (let i = 0; i < loops; i++) {
            for (const entity of [...input.attackingBoard]) {
                if (entity.entityId === input.attacker.entityId) {
                    continue;
                }
                const isSameSide = entity.friendly === input.attacker.friendly;
                const board = isSameSide ? input.attackingBoard : input.defendingBoard;
                const hero = isSameSide ? input.attackingHero : input.defendingHero;
                (0, attack_1.dealDamageToMinion)(entity, board, hero, input.attacker, 1, isSameSide ? input.defendingBoard : input.attackingBoard, isSameSide ? input.defendingHero : input.attackingHero, input.gameState);
            }
        }
        return { dmgDoneByAttacker: 0, dmgDoneByDefender: 0 };
    },
};
//# sourceMappingURL=rampager.js.map