"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SelflessHero = void 0;
const divine_shield_1 = require("../../../keywords/divine-shield");
exports.SelflessHero = {
    cardIds: ["BG_OG_221", "TB_BaconUps_014"],
    battlecry: (minion, input) => {
        var _a;
        if (!((_a = input.hero.trinkets) === null || _a === void 0 ? void 0 : _a.some((t) => t.cardId === "BG32_MagicItem_804"))) {
            return false;
        }
        const mult = minion.cardId === "TB_BaconUps_014" ? 2 : 1;
        for (let i = 0; i < mult; i++) {
            (0, divine_shield_1.grantRandomDivineShield)(minion, input.board, input.hero, input.otherHero, input.gameState);
        }
        return true;
    },
    deathrattleSpawn: (minion, input) => {
        const mult = minion.cardId === "TB_BaconUps_014" ? 2 : 1;
        for (let i = 0; i < mult; i++) {
            (0, divine_shield_1.grantRandomDivineShield)(minion, input.boardWithDeadEntity, input.boardWithDeadEntityHero, input.otherBoardHero, input.gameState);
        }
        return [];
    },
};
//# sourceMappingURL=selfless-hero.js.map