"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Stuntdrake = void 0;
const utils_1 = require("../../../services/utils");
const stats_1 = require("../../../simulation/stats");
exports.Stuntdrake = {
    cardIds: ["BG34_732", "BG34_732_G"],
    baseAvengeValue: (cardId) => 3,
    avenge: (minion, input) => {
        const loops = minion.cardId === "BG34_732_G" ? 2 : 1;
        for (let i = 0; i < loops; i++) {
            const picked = [];
            for (let j = 0; j < 2; j++) {
                const possibleTargets = input.board.filter((e) => !exports.Stuntdrake.cardIds.includes(e.cardId) && !picked.includes(e));
                const target = (0, utils_1.pickRandom)(possibleTargets);
                if (!!target) {
                    (0, stats_1.modifyStats)(target, minion, minion.attack, 0, input.board, input.hero, input.gameState);
                    picked.push(target);
                }
            }
        }
    },
};
//# sourceMappingURL=stuntdrake.js.map