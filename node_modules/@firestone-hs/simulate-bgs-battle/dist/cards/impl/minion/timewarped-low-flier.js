"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimewarpedLowFlier = void 0;
const stats_1 = require("../../../simulation/stats");
exports.TimewarpedLowFlier = {
    cardIds: ["BG34_Giant_065", "BG34_Giant_065_G"],
    endOfTurn: (minion, input) => {
        const mult = minion.cardId === "BG34_Giant_065_G" ? 2 : 1;
        const attackTargets = input.board.filter((e) => e.attack < minion.attack);
        for (const target of attackTargets) {
            (0, stats_1.modifyStats)(target, minion, 2 * mult, 0, input.board, input.hero, input.gameState);
        }
        const healthTargets = input.board.filter((e) => e.health < minion.health);
        for (const target of healthTargets) {
            (0, stats_1.modifyStats)(target, minion, 0, 2 * mult, input.board, input.hero, input.gameState);
        }
    },
};
//# sourceMappingURL=timewarped-low-flier.js.map