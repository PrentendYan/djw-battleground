"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RighteousCharge = void 0;
const divine_shield_1 = require("../../../keywords/divine-shield");
const attack_1 = require("../../../simulation/attack");
exports.RighteousCharge = {
    cardIds: ["BG33_Reward_003"],
    startOfCombat: (trinket, input) => {
        if (input.playerBoard.length > 0) {
            const target = input.playerBoard[0];
            if (!!target) {
                input.gameState.spectator.registerPowerTarget(trinket, target, input.playerBoard, input.playerEntity, input.opponentEntity);
                (0, divine_shield_1.updateDivineShield)(target, input.playerBoard, input.playerEntity, input.opponentEntity, true, input.gameState);
                target.attackImmediately = true;
                const previousHasAttacked = target.hasAttacked;
                (0, attack_1.simulateAttack)(input.playerBoard, input.playerEntity, input.opponentBoard, input.opponentEntity, input.gameState);
                target.hasAttacked = previousHasAttacked;
                target.attackImmediately = false;
            }
            return true;
        }
    },
};
//# sourceMappingURL=righteous-charge.js.map