"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimewarpedShoal = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const utils_1 = require("../../../services/utils");
const stats_1 = require("../../../simulation/stats");
const utils_2 = require("../../../utils");
exports.TimewarpedShoal = {
    cardIds: [
        "BG34_Giant_210t",
        "BG34_Giant_210_Gt",
    ],
    castTavernSpell: (spellCardId, input) => {
        var _a;
        const mult = spellCardId === "BG34_Giant_210_Gt" ? 2 : 1;
        const nagas = input.board.filter((entity) => (0, utils_2.hasCorrectTribe)(entity, input.hero, reference_data_1.Race.NAGA, input.gameState.anomalies, input.gameState.allCards));
        const buff = 2 * nagas.length * mult;
        const target = (_a = input.target) !== null && _a !== void 0 ? _a : (0, utils_1.pickRandom)(input.board);
        if (!!target) {
            (0, stats_1.modifyStats)(target, input.source, buff, buff, input.board, input.hero, input.gameState);
            input.gameState.spectator.registerPowerTarget(input.source, target, input.board, input.hero, input.otherHero);
        }
    },
};
//# sourceMappingURL=timewarped-shoal.js.map