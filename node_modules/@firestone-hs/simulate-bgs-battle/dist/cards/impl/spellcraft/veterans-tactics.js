"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.VeteransTactics = void 0;
const utils_1 = require("../../../utils");
const cards_data_1 = require("../../cards-data");
exports.VeteransTactics = {
    cardIds: [
        "BG34_407t",
        "BG34_407_Gt",
    ],
    castTavernSpell: (spellCardId, input) => {
        let totalKeywords = 0;
        for (const bonusKeyword of cards_data_1.validBonusKeywords) {
            if (input.board.some((e) => (0, cards_data_1.hasKeyword)(e, bonusKeyword))) {
                totalKeywords++;
            }
        }
        const loops = totalKeywords;
        const mult = spellCardId === "BG34_407_Gt" ? 2 : 1;
        for (let i = 0; i < loops; i++) {
            (0, utils_1.addStatsToBoard)(input.source, input.board, input.hero, 2 * mult, 1 * mult, input.gameState);
        }
    },
};
//# sourceMappingURL=veterans-tactics.js.map