import { BgsPlayerEntity, BoardTrinket } from '../bgs-player-entity';
import { BoardEntity } from '../board-entity';
import { FullGameState } from './internal-game-state';
export declare const playBloodGemsOn: (source: BoardEntity | BoardTrinket | BgsPlayerEntity, target: BoardEntity, quantity: number, board: BoardEntity[], hero: BgsPlayerEntity, otherBoard: BoardEntity[], otherHero: BgsPlayerEntity, gameState: FullGameState, registerTarget?: boolean) => void;
export declare const applyBloodGemEnchantment: (enchantmentCardId: string, target: BoardEntity, source: BoardEntity | BoardTrinket | BgsPlayerEntity, quantity: number, bloodGemAttack: number, bloodGemHealth: number) => void;
export interface PlayedBloodGemsOnMeInput {
    board: BoardEntity[];
    hero: BgsPlayerEntity;
    otherBoard: BoardEntity[];
    otherHero: BgsPlayerEntity;
    gameState: FullGameState;
}
export interface PlayedBloodGemsOnAnyInput {
    source: BoardEntity | BoardTrinket | BgsPlayerEntity;
    target: BoardEntity;
    board: BoardEntity[];
    hero: BgsPlayerEntity;
    otherBoard: BoardEntity[];
    otherHero: BgsPlayerEntity;
    gameState: FullGameState;
}
