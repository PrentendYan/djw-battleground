import { BgsPlayerEntity } from '../bgs-player-entity';
import { BoardEntity } from '../board-entity';
import { FullGameState } from './internal-game-state';
export declare const addCardsInHand: (playerEntity: BgsPlayerEntity, board: BoardEntity[], cardsAdded: readonly any[], gameState: FullGameState) => BoardEntity[];
export declare const removeCardFromHand: (playerEntity: BgsPlayerEntity, card: BoardEntity) => void;
export interface OnCardAddedToHandInput {
    addedCard: BoardEntity;
    board: BoardEntity[];
    hero: BgsPlayerEntity;
    gameState: FullGameState;
}
