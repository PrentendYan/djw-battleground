import { BgsPlayerEntity } from '../bgs-player-entity';
import { BoardEntity } from '../board-entity';
import { FullGameState } from './internal-game-state';
export declare const onEntityDamaged: (damagedEntity: BoardEntity, board: BoardEntity[], hero: BgsPlayerEntity, otherBoard: BoardEntity[], otherHero: BgsPlayerEntity, damageSource: BoardEntity | BgsPlayerEntity, damage: number, gameState: FullGameState) => readonly BoardEntity[];
export interface AfterDealDamageInput {
    damagedEntity: BoardEntity | BgsPlayerEntity;
    damageDealer: BoardEntity | BgsPlayerEntity;
    damage: number;
    board: BoardEntity[];
    hero: BgsPlayerEntity;
    gameState: FullGameState;
}
