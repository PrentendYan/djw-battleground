"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.onDeathrattleTriggered = void 0;
const card_interface_1 = require("../cards/card.interface");
const _card_mappings_1 = require("../cards/impl/_card-mappings");
const quest_1 = require("./quest");
const onDeathrattleTriggered = (input) => {
    var _a, _b;
    for (const boardEntity of input.boardWithDeadEntity) {
        const onDeathrattleTriggered = _card_mappings_1.cardMappings[boardEntity.cardId];
        if ((0, card_interface_1.hasOnDeathrattleTriggered)(onDeathrattleTriggered)) {
            onDeathrattleTriggered.onDeathrattleTriggered(boardEntity, input);
        }
    }
    for (const trinket of (_a = input.boardWithDeadEntityHero.trinkets) !== null && _a !== void 0 ? _a : []) {
        const onDeathrattleTriggered = _card_mappings_1.cardMappings[trinket.cardId];
        if ((0, card_interface_1.hasOnDeathrattleTriggered)(onDeathrattleTriggered)) {
            onDeathrattleTriggered.onDeathrattleTriggered(trinket, input);
        }
    }
    const quests = (_b = input.boardWithDeadEntityHero.questEntities) !== null && _b !== void 0 ? _b : [];
    for (const quest of quests) {
        switch (quest.CardId) {
            case "BG24_Quest_320":
                (0, quest_1.onQuestProgressUpdated)(input.boardWithDeadEntityHero, quest, input.boardWithDeadEntity, input.gameState);
                break;
        }
    }
    input.boardWithDeadEntityHero.trinkets
        .filter((t) => t.cardId === "BG30_MagicItem_700")
        .forEach((t) => {
        t.scriptDataNum1 = 0;
    });
};
exports.onDeathrattleTriggered = onDeathrattleTriggered;
//# sourceMappingURL=deathrattle-on-trigger.js.map