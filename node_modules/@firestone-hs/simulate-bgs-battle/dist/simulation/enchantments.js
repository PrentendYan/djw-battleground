"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fixEnchantments = void 0;
const reference_data_1 = require("@firestone-hs/reference-data");
const fixEnchantments = (entity, allCards) => {
    var _a;
    const newEnchantments = ((_a = entity.enchantments) !== null && _a !== void 0 ? _a : [])
        .map((enchantment) => isNaN(+enchantment.cardId)
        ? enchantment
        : {
            ...enchantment,
            cardId: getEnchantmentForDbfId(+enchantment.cardId, allCards),
        })
        .map((enchantment, index) => ({
        ...enchantment,
        timing: enchantment.timing || entity.entityId + index + 1,
    }));
    return {
        ...entity,
        enchantments: newEnchantments,
        pendingAttackBuffs: [],
    };
};
exports.fixEnchantments = fixEnchantments;
const getEnchantmentForDbfId = (dbfId, allCards) => {
    var _a, _b, _c, _d;
    const refCard = allCards.getCard(dbfId);
    if (((_a = refCard.type) === null || _a === void 0 ? void 0 : _a.toUpperCase()) === reference_data_1.CardType[reference_data_1.CardType.ENCHANTMENT]) {
        return refCard.id;
    }
    return (_c = (_b = allCards.getCard(refCard.enchantmentDbfId)) === null || _b === void 0 ? void 0 : _b.id) !== null && _c !== void 0 ? _c : (_d = allCards.getCard(dbfId)) === null || _d === void 0 ? void 0 : _d.id;
};
//# sourceMappingURL=enchantments.js.map