import { SingleSimulationResult } from '../single-simulation-result';
import { FullGameState, PlayerState } from './internal-game-state';
export declare class Simulator {
    private readonly gameState;
    private currentAttacker;
    private currentSpeedAttacker;
    private hasShowShortCircuitWarning;
    constructor(gameState: FullGameState);
    simulateSingleBattle(playerState: PlayerState, opponentState: PlayerState): SingleSimulationResult;
    private simulateSingleBattlePass;
    private buildBoardTotalDamage;
}
