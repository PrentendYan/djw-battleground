import { AllCardsService } from '@firestone-hs/reference-data';
import { BgsPlayerEntity, BoardTrinket } from '../../bgs-player-entity';
import { BoardEntity } from '../../board-entity';
import { FullGameState } from '../internal-game-state';
export declare const makeMinionGolden: (target: BoardEntity, source: BoardEntity | BgsPlayerEntity | BoardTrinket, targetBoard: BoardEntity[], targetBoardHero: BgsPlayerEntity, otherBoard: BoardEntity[], otherHero: BgsPlayerEntity, gameState: FullGameState) => void;
export declare const isMinionGolden: (entity: BoardEntity, allCards: AllCardsService) => boolean;
